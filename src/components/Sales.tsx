import {FC} from 'react';
import {Text, View} from 'react-native';

interface Props {}

const Sales: FC<Props> = ({}) => {
  return (
    <View>
      <Text>sales</Text>
    </View>
  );
};

export default Sales;
